---
phase: quick
plan: 260331-rj9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/decisions/DecisionQueue.tsx
  - src/app/(app)/decisions/DecisionCard.tsx
  - src/app/(app)/decisions/[id]/OptionList.tsx
autonomous: true
must_haves:
  truths:
    - "When createDecision fails, user sees inline error text below the submit button"
    - "When addOption fails, user sees inline error text below the add button"
    - "When resolveDecision fails, user sees inline error text near the resolve trigger"
    - "While reorder is in-flight, the reorder buttons are visually disabled"
  artifacts:
    - path: "src/app/(app)/decisions/DecisionQueue.tsx"
      provides: "Error display for createDecision, isPending passed to DecisionCard"
    - path: "src/app/(app)/decisions/DecisionCard.tsx"
      provides: "Reorder buttons disabled when isPending=true"
    - path: "src/app/(app)/decisions/[id]/OptionList.tsx"
      provides: "Error display for addOption and resolveDecision"
  key_links:
    - from: "DecisionQueue.tsx"
      to: "DecisionCard.tsx"
      via: "isPending prop"
      pattern: "isPending.*boolean"
---

<objective>
Fix two UX issues: (1) silently discarded mutation errors in DecisionQueue, OptionList, and (2) missing pending visual feedback on reorder buttons in DecisionCard.

Purpose: Users currently get no feedback when server actions fail, and no visual cue that reorder is processing.
Output: Updated components with inline error states and disabled-during-transition reorder buttons.
</objective>

<execution_context>
@/Users/aaronwoo/claude/twogether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronwoo/claude/twogether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/(app)/decisions/DecisionQueue.tsx
@src/app/(app)/decisions/DecisionCard.tsx
@src/app/(app)/decisions/[id]/OptionList.tsx
@src/app/(app)/decisions/[id]/OptionCard.tsx (reference pattern for error handling)
</context>

<interfaces>
<!-- OptionCard.tsx error pattern to replicate -->
From src/app/(app)/decisions/[id]/OptionCard.tsx:
```tsx
const [error, setError] = useState<string | null>(null);
// ...
setError(null);
const result = await upsertVote(...);
if (result?.error) {
  setError(result.error);
}
// ...
{error && <p className="text-sm text-destructive">{error}</p>}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Surface mutation errors in DecisionQueue and OptionList</name>
  <files>src/app/(app)/decisions/DecisionQueue.tsx, src/app/(app)/decisions/[id]/OptionList.tsx</files>
  <action>
In DecisionQueue.tsx:
- Add `const [error, setError] = useState<string | null>(null)` state.
- In `handleAdd`, before the `startTransition` call, call `setError(null)`.
- In the `startTransition` callback, add an `else` branch: `else { setError(result?.error ?? "Failed to create decision"); }`.
- Below the Submit/Cancel button row (after the `<div className="flex gap-2">` closing div, still inside CardContent), add: `{error && <p className="text-sm text-destructive">{error}</p>}`.
- Also clear error on `handleCancel`: add `setError(null)` in that function.

In OptionList.tsx:
- Add `const [addError, setAddError] = useState<string | null>(null)` and `const [resolveError, setResolveError] = useState<string | null>(null)` states.
- In `handleAdd` startTransition callback: call `setAddError(null)` before the action. In the result check, add else branch: `else { setAddError(result?.error ?? "Failed to add option"); }`.
- In `handleResolve` startTransition callback: call `setResolveError(null)` before the action. Capture result: `const result = await resolveDecision(decision.id, optionId);`. Add: `if (result?.error) { setResolveError(result.error); }`. On success path (no error), call `router.refresh()`.
- Below the add option input row, add: `{addError && <p className="text-sm text-destructive mt-1">{addError}</p>}`.
- Above the options list (below the header section, before the options map), add: `{resolveError && <p className="text-sm text-destructive mb-2">{resolveError}</p>}`.
- Clear addError on `handleCancel`: `setAddError(null)`.

Follow the same error display pattern as OptionCard.tsx: `<p className="text-sm text-destructive">`.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/claude/twogether && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three mutation paths (createDecision, addOption, resolveDecision) display error text to the user when the server action returns an error. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Surface isPending for reorder buttons via DecisionCard</name>
  <files>src/app/(app)/decisions/DecisionQueue.tsx, src/app/(app)/decisions/DecisionCard.tsx</files>
  <action>
In DecisionQueue.tsx:
- Change `const [, startTransition] = useTransition()` to `const [isPending, startTransition] = useTransition()`.
- Pass `isPending` as a new prop to each DecisionCard: `isReordering={isPending}`.

In DecisionCard.tsx:
- Add `isReordering: boolean` to the DecisionCardProps interface.
- Accept `isReordering` in the component destructure.
- On both reorder buttons (ChevronUp and ChevronDown), add `isReordering` to the disabled condition:
  - Up button: `disabled={isFirst || isReordering}`
  - Down button: `disabled={isLast || isReordering}`
- Add `opacity-50` class when `isReordering` is true to provide visual feedback. Use a conditional: `className={cn("w-[40px] ...", isReordering && "opacity-50")}` -- or use inline ternary if cn is not imported. Simplest approach: add to the existing className string with a ternary, e.g., `className={"w-[40px] bg-blue-100 hover:bg-blue-200 text-blue-700" + (isReordering ? " opacity-50" : "")}`.

Note: isPending from useTransition is true for ALL concurrent transitions, so while any reorder is in flight, all reorder buttons will be disabled. This is acceptable and prevents race conditions from rapid clicking.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/claude/twogether && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Reorder buttons show disabled + opacity-50 state while any reorder server action is in flight. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run lint` passes
3. Visual check: create a decision with server error condition -> error text appears. Click reorder -> buttons dim during transition.
</verification>

<success_criteria>
- All mutation error paths in DecisionQueue and OptionList show user-visible error text
- Reorder buttons are disabled with visual feedback during in-flight transitions
- No TypeScript or lint errors introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260331-rj9-surface-mutation-errors-and-pending-stat/260331-rj9-SUMMARY.md`
</output>
