---
phase: 04-budget-tracking
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/budget/BudgetClient.tsx
autonomous: true
requirements:
  - BUDG-02
  - BUDG-05
gap_closure: true

must_haves:
  truths:
    - "Over-budget warning banner appears when allocated total exceeds total budget, even when totalBudget is 0"
    - "Pending status toggle button shows amber styling (bg-amber-100 text-amber-700 border-amber-300) when selected"
    - "All cancel buttons in delete confirmations and edit forms are labeled 'Cancel', not 'Discard'"
  artifacts:
    - path: "src/app/(app)/budget/BudgetClient.tsx"
      provides: "Fixed isOverAllocated condition, amber Pending button styles, 'Cancel' button labels"
      contains: "isOverAllocated = allocated > wedding.totalBudget"
  key_links:
    - from: "src/app/(app)/budget/BudgetClient.tsx"
      to: "isOverAllocated banner"
      via: "line 87 condition"
      pattern: "isOverAllocated = allocated > wedding\\.totalBudget"
---

<objective>
Fix three issues in BudgetClient.tsx: remove the `&& wedding.totalBudget > 0` guard that suppresses the over-budget warning when total budget is unset, update the Pending status toggle button to use amber styling matching the Pending badge, and rename all "Discard" cancel buttons to "Cancel".

Purpose: These are UAT failures from 04-UAT.md (tests 4, 9, 10, 11). They are all isolated to BudgetClient.tsx with no schema or server action changes needed.

Output: Updated BudgetClient.tsx with 3 targeted fixes.
</objective>

<execution_context>
@/Users/aaronwoo/claude/twogether/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aaronwoo/claude/twogether/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-budget-tracking/04-UAT.md

<interfaces>
<!-- Current state of BudgetClient.tsx relevant to each fix -->

Fix 1 — isOverAllocated condition (line 87):
```typescript
// CURRENT (broken — suppresses warning when budget is 0):
const isOverAllocated = allocated > wedding.totalBudget && wedding.totalBudget > 0;

// TARGET (correct):
const isOverAllocated = allocated > wedding.totalBudget;
```

Fix 2 — Pending button active class (two locations):

Location A: edit-expense form (lines ~534-554):
```typescript
// CURRENT (both Pending and Paid use same sage-500 active class):
className={`px-3 py-1 rounded text-sm border ${
  editExpStatus === "pending"
    ? "bg-sage-500 text-white border-sage-500"
    : "border border-border text-muted-foreground"
}`}

// TARGET (Pending gets amber, Paid keeps sage):
className={`px-3 py-1 rounded text-sm border ${
  editExpStatus === "pending"
    ? "bg-amber-100 text-amber-700 border-amber-300"
    : "border border-border text-muted-foreground"
}`}
// Paid button stays:
className={`px-3 py-1 rounded text-sm border ${
  editExpStatus === "paid"
    ? "bg-sage-500 text-white border-sage-500"
    : "border border-border text-muted-foreground"
}`}
```

Location B: add-expense form (lines ~651-670) — same pattern, same fix using `expStatus` instead of `editExpStatus`.

Fix 3 — "Discard" → "Cancel" (all occurrences):
Occurrences in BudgetClient.tsx (verified by reading file):
- Line ~349: new-category form cancel button — `>Discard</Button>`
- Line ~402: delete-category confirmation cancel button — `>Discard</Button>`
- Line ~429: edit-category form cancel button — `>Discard</Button>`
- Line ~509: delete-expense confirmation cancel button — `>Discard</Button>`
- Line ~576: edit-expense form cancel button — `>Discard</Button>`
- Line ~700: add-expense form cancel button — `>Discard</Button>`

Change every `>Discard</Button>` to `>Cancel</Button>` in the file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix isOverAllocated condition and amber Pending button styles</name>
  <files>src/app/(app)/budget/BudgetClient.tsx</files>
  <action>
Read src/app/(app)/budget/BudgetClient.tsx in full before making any edits.

**Fix 1 — Remove the `&& wedding.totalBudget > 0` guard (line 87):**

Find this line:
```typescript
const isOverAllocated = allocated > wedding.totalBudget && wedding.totalBudget > 0;
```

Replace with:
```typescript
const isOverAllocated = allocated > wedding.totalBudget;
```

This makes the banner appear whenever allocated > totalBudget, including when totalBudget is 0.

**Fix 2 — Pending button active class in edit-expense form (~lines 534-554):**

Find the `editExpStatus === "pending"` branch in the status toggle group inside the edit-expense form. The current Pending button active class is `bg-sage-500 text-white border-sage-500`. Change it to `bg-amber-100 text-amber-700 border-amber-300`.

The Paid button (`editExpStatus === "paid"` branch) keeps `bg-sage-500 text-white border-sage-500` — do NOT change it.

After the edit the Pending button className template string should read:
```
editExpStatus === "pending"
  ? "bg-amber-100 text-amber-700 border-amber-300"
  : "border border-border text-muted-foreground"
```

**Fix 3 — Pending button active class in add-expense form (~lines 651-670):**

Find the `expStatus === "pending"` branch in the status toggle group inside the add-expense form. Same change: active class from `bg-sage-500 text-white border-sage-500` to `bg-amber-100 text-amber-700 border-amber-300`.

The Paid button (`expStatus === "paid"`) keeps sage-500. Do NOT change it.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/claude/twogether && npx tsc --noEmit</automated>
  </verify>
  <done>
- Line 87 reads `const isOverAllocated = allocated > wedding.totalBudget;` (no `&& wedding.totalBudget > 0`)
- Both Pending toggle buttons have `bg-amber-100 text-amber-700 border-amber-300` as their active class
- Both Paid toggle buttons still have `bg-sage-500 text-white border-sage-500`
- `npx tsc --noEmit` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all "Discard" button labels with "Cancel"</name>
  <files>src/app/(app)/budget/BudgetClient.tsx</files>
  <action>
Read src/app/(app)/budget/BudgetClient.tsx. Find every `>Discard</Button>` occurrence and change it to `>Cancel</Button>`.

There are 6 occurrences to change:
1. New-category form cancel button (~line 349)
2. Delete-category confirmation cancel button (~line 402)
3. Edit-category form cancel button (~line 429)
4. Delete-expense confirmation cancel button (~line 509)
5. Edit-expense form cancel button (~line 576)
6. Add-expense form cancel button (~line 700)

Do a global find-and-replace of `>Discard</Button>` with `>Cancel</Button>` across the entire file. Confirm the count is exactly 6 replacements.

Do not change any other text. The button variants (`ghost`) and handlers are unchanged.
  </action>
  <verify>
    <automated>cd /Users/aaronwoo/claude/twogether && ! grep -q "Discard" src/app/\(app\)/budget/BudgetClient.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>
- `grep -q "Discard" src/app/(app)/budget/BudgetClient.tsx` finds no matches (exits 1, negated to 0)
- All 6 cancel buttons now display "Cancel"
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
  </done>
</task>

</tasks>

<verification>
- `grep -c "Discard" src/app/(app)/budget/BudgetClient.tsx` returns 0
- `grep "isOverAllocated" src/app/(app)/budget/BudgetClient.tsx` shows `allocated > wedding.totalBudget` without `&& wedding.totalBudget > 0`
- `grep "bg-amber-100 text-amber-700" src/app/(app)/budget/BudgetClient.tsx` returns 2 (one per status toggle group)
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
1. Over-budget warning appears when allocated > totalBudget even when totalBudget is $0
2. Pending toggle button is amber-colored in both add-expense and edit-expense forms
3. Paid toggle button remains sage-colored in both forms
4. All cancel buttons across the entire file say "Cancel"
5. TypeScript and lint pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-budget-tracking/04-03-SUMMARY.md`
</output>
