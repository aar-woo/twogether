---
phase: 4
slug: budget-tracking
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-06
---

# Phase 4 — UI Design Contract: Budget Tracking

> Visual and interaction contract for the /budget page. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json |
| Preset | base-nova | components.json `style` field |
| Base color | neutral | components.json `tailwind.baseColor` |
| CSS variables | true | components.json |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | lucide-react | components.json `iconLibrary` |
| Serif font | Playfair Display (--font-serif) | globals.css |
| Sans font | Inter (--font-sans) | globals.css |

### shadcn Components Available (installed)

| Component | File | Phase 4 Use |
|-----------|------|-------------|
| Card, CardHeader, CardTitle, CardContent | components/ui/card.tsx | Summary bar stat cards, accordion headers, expense rows |
| Button | components/ui/button.tsx | All action triggers |
| Input | components/ui/input.tsx | Category name, allocated amount, vendor name, amount, date |
| Label | components/ui/label.tsx | Form field labels |
| Textarea | components/ui/textarea.tsx | Expense note field |

No new shadcn components need to be installed for this phase.

### Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Button, Input, Label, Textarea | not required — all pre-installed |

No third-party registries declared. Registry vetting gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding, inline button gaps |
| sm | 8px | Form field stacking gaps, compact row padding |
| md | 16px | Card padding (px-4), section element spacing |
| lg | 24px | Section vertical gap (mb-6), heading-to-content gap |
| xl | 32px | Major section separators (mb-8) |
| 2xl | 48px | Page vertical spacing |
| 3xl | 64px | Empty state vertical padding |

Exceptions:
- Touch target minimum 44px height for action buttons in expense rows (accessibility)
- Accordion header row: 48px minimum height to ensure comfortable click/tap area

---

## Typography

All from the established project type system (source: dashboard/page.tsx, DecisionQueue.tsx).

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Page heading | 24px (text-2xl) | 400 regular | 1.2 | Playfair Display (font-serif) | "Budget" page title |
| Section heading | 20px (text-xl) | 400 regular | 1.2 | Playfair Display (font-serif) | Section sub-headings if needed |
| Body / label | 16px (text-base) | 400 regular | 1.5 | Inter (font-sans) | Category names, expense vendor names, form labels |
| Caption / metadata | 14px (text-sm) | 400 regular | 1.5 | Inter (font-sans) | Expense date, note, muted helper text, badge text |
| Stat value | 24px (text-2xl) | 600 semibold | 1.2 | Inter (font-sans) | Summary bar currency amounts (matches dashboard cards) |
| Stat label | 12px (text-xs) | 500 medium | 1.3 | Inter (font-sans) | Summary bar card labels, uppercase tracking-wide (matches dashboard) |

Font weight constraint: 2 weights in use — regular (400) for all body/display content, semibold (600) for numeric stat values only.

---

## Color

Source: globals.css sage green palette + shadcn CSS variable aliases.

| Role | Value | Class | Usage |
|------|-------|-------|-------|
| Dominant surface (60%) | oklch(98.5% 0.005 80) | bg-background | Page background |
| Secondary surface (30%) | oklch(100% 0 0) | bg-card | Stat cards, accordion header rows, expense row hover surface |
| Muted surface | oklch(96% 0.008 80) | bg-muted | Collapsed accordion row background, inline form background |
| Primary (sage-500) | #4A8059 | text-primary, bg-primary | Primary action buttons (Add Category, Save) |
| Foreground | #1E3325 | text-foreground | All primary text: category names, vendor names, amounts |
| Muted foreground | #3D6B4A | text-muted-foreground | Secondary text: labels, date, note preview |
| Border | oklch(88% 0.01 80) | border-border | Card borders, accordion dividers, input borders |

### Accent (10%) — Reserved For

Accent is `#C8E3CC` (sage-100 / --secondary). Reserved exclusively for:
- Active/open accordion indicator (left border stripe: `border-l-4 border-sage-400`)
- Focus ring on form inputs (via `--ring: #4A8059`)

Accent is NOT used for general interactive states or hover backgrounds.

### Semantic Colors

| Role | Value | Class | Reserved For |
|------|-------|-------|-------------|
| Paid badge | #4A8059 / sage-100 bg | bg-sage-100 text-sage-700 | Paid status badge only |
| Pending badge | amber-100 bg, amber-700 text | bg-amber-100 text-amber-700 | Pending status badge only |
| Over-budget warning | amber-50 bg, amber-700 text | bg-amber-50 text-amber-700 border-amber-200 | Over-budget inline warning banner only |
| Destructive | oklch(0.58 0.22 27) | text-destructive, bg-destructive | Delete confirm button, action error messages |

### Color Notes

- Never use `terracotta-*` classes — palette migrated to sage green in quick task 260406-qvk
- Never use arbitrary CSS variable syntax `bg-[--color-sage-500]` — use utility class `bg-sage-500`
- The amber semantic color (pending, over-budget) is a deliberate warm neutral that contrasts with sage without introducing a new brand color

---

## Component Interaction Contracts

### Summary Stat Bar (BUDG-01, BUDG-06)

Layout: 4-column grid (`grid grid-cols-2 lg:grid-cols-4 gap-4`) mirroring dashboard budget cards exactly.

Cards: Total Budget / Allocated / Spent / Remaining. Reuse shadcn Card + CardHeader + CardContent structure from `dashboard/page.tsx`. Stat value: `text-2xl font-semibold`. Label: `text-xs font-medium text-muted-foreground uppercase tracking-wide`.

Currency formatting: `Intl.NumberFormat("en-US", { style: "currency", currency: "USD", minimumFractionDigits: 0 })` — same helper as dashboard.

### Over-Budget Warning (BUDG-02)

Placement: Directly below the summary stat bar, above the `+ New Category` button and category list.

Trigger: Client-side computed — `sum(categories.allocated_amount) > wedding.total_budget` AND `wedding.total_budget > 0`.

Visual: Inline banner `bg-amber-50 border border-amber-200 text-amber-700 rounded-md px-3 py-2 text-sm`. Not a modal, not a toast.

Copy: "Allocated total exceeds your budget by [amount]." (amount formatted as currency)

Dismiss: Not dismissable — warning stays visible while condition is true.

### Category Accordion (D-02, D-03)

State: Single-open controlled via `openCategoryId: string | null` — null means all collapsed (default on load per D-03).

Collapsed header shows: category name (body weight), allocated amount (muted), spent total (muted). Right side: edit icon button, chevron icon (down when collapsed, up when expanded).

Expanded: body area renders expense list rows + inline add form at bottom.

Opening one accordion does NOT force-close others — each can open/close independently (multi-open allowed; simpler state model than single-open).

Transition: No CSS animation required — conditional render is sufficient.

### Category Create Form (D-04)

Trigger: `+ New Category` outline button above accordion list. On click, expands an inline Card form above the list (not modal).

Form fields:
- Category name: Input, placeholder "Category name", required
- Allocated amount: Input type="text" (formatted on blur), placeholder "$0", required

Buttons: `Add Category` (primary, sm) / `Cancel` (ghost, sm). Inline error below buttons on failure.

Cancel: Collapses form, clears fields, no state change.

Submit: On success — collapses form, clears fields, router.refresh(). On error — shows error message in `text-sm text-destructive`.

### Category Inline Edit (D-05)

Trigger: Edit icon (lucide `Pencil`, 16px) on the accordion header row. Visible on hover.

Edit state: Name and allocated amount fields replace display text in-place in the accordion header. Save / Cancel buttons appear inline.

Save: Calls updateCategory server action, clears `editingCategoryId`, router.refresh(). On error — error text below the fields.

Cancel: Clears `editingCategoryId`, restores display mode, no server call.

### Expense Row Display (BUDG-05)

Each row: vendor name (body) + amount (body, right-aligned) + status badge + optional date (caption, muted).

Status badge: pill shape, `text-xs px-2 py-0.5 rounded-full`. Paid: `bg-sage-100 text-sage-700`. Pending: `bg-amber-100 text-amber-700`.

Row hover: subtle `hover:bg-muted` background transition for click affordance (D-08 click-to-edit).

Note preview: If expense has a note, show first 60 chars as `text-xs text-muted-foreground` below the primary row.

### Expense Inline Add Form (D-06, D-07)

Trigger: `+ Add expense` text button at the bottom of an expanded expense list. Expands inline form below existing rows.

Required fields:
- Vendor name: Input, placeholder "Vendor name"
- Amount: Input, placeholder "$0.00"
- Status: Two-button toggle (not a select) — "Pending" / "Paid". Default: Pending. Active state: `bg-sage-500 text-white`, inactive: `border border-border text-muted-foreground`

Optional fields (collapsed under "More details" disclosure — not a modal, just conditional render):
- Date: Input type="date"
- Note: Textarea, placeholder "Optional note", 2 rows

Buttons: `Add Expense` (primary, sm) / `Cancel` (ghost, sm). Inline error on failure.

### Expense Inline Edit (D-08)

Trigger: Click anywhere on an expense row (except the delete icon).

Edit state: Row expands into full edit form in place (same fields as add form, pre-filled). Save / Cancel buttons.

One row editable at a time — opening edit on a second row closes the first without saving.

### Delete Confirmation

Applies to: category delete, expense delete.

Behavior: Inline confirm pattern — no modal, no toast library.

Category delete: Delete icon (lucide `Trash2`, 16px) on accordion header. Click transitions row to: `[Category name] — Delete category and all expenses? [Delete] [Cancel]`. Text in `text-sm text-destructive`. Confirm button: `variant="destructive" size="sm"`.

Expense delete: Delete icon (lucide `Trash2`, 14px) at right edge of expense row, visible on hover. Click transitions the row to: `[vendor name] — Delete expense? [Delete] [Cancel]` inline. Same destructive button style.

Rationale: Inline confirm avoids requiring a toast library. The cascade warning for categories (all expenses also deleted) is surfaced in the confirmation copy.

### Empty State

Trigger: No budget categories exist yet.

Layout: Centered vertically in the main content area (`py-16 text-center`).

Content:
- Heading (text-lg font-serif): "No budget categories yet"
- Body (text-sm text-muted-foreground): "Add a category to start tracking your wedding expenses."
- CTA: `+ New Category` primary button (same as header button — either triggers the same inline form state)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page heading | "Budget" |
| Primary CTA | "+ New Category" |
| Category add form submit | "Add Category" |
| Category save edit | "Save" |
| Expense add trigger | "+ Add expense" |
| Expense add form submit | "Add Expense" |
| Expense save edit | "Save" |
| Cancel (all contexts) | "Cancel" |
| Empty state heading | "No budget categories yet" |
| Empty state body | "Add a category to start tracking your wedding expenses." |
| Over-budget warning | "Allocated total exceeds your budget by [amount]." |
| Category delete confirmation | "Delete category and all its expenses?" |
| Expense delete confirmation | "Delete this expense?" |
| Delete button label | "Delete" |
| Error — required field missing | "Please fill in all required fields." |
| Error — invalid amount | "Amount must be a positive number." |
| Error — server failure | "Something went wrong. Please try again." |
| Status badge — paid | "Paid" |
| Status badge — pending | "Pending" |
| Accordion — allocated label | "Allocated" |
| Accordion — spent label | "Spent" |

---

## Layout Contract

Page container: `max-w-5xl mx-auto px-6 py-8` (inherited from `(app)/layout.tsx` main element — do not re-wrap).

Budget page inner layout (top to bottom):
1. Page heading: `font-serif text-2xl text-foreground mb-6`
2. Summary stat bar: `grid grid-cols-2 lg:grid-cols-4 gap-4 mb-4`
3. Over-budget warning (conditional): `mb-4`
4. Category list header row: `+ New Category` button right-aligned, `mb-4`
5. Inline new-category form (conditional): `mb-4`
6. Category accordion list: stacked, `space-y-2`
7. Empty state (conditional, when no categories): `py-16 text-center`

Accordion row layout: `flex items-center justify-between px-4 py-3 cursor-pointer`

Expense list: `divide-y divide-border` inside expanded accordion body.

Expense row layout: `flex items-center gap-3 px-4 py-3`

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Sources

| Decision / Value | Source |
|-----------------|--------|
| Summary stat bar layout (4 cards) | CONTEXT.md D-01 |
| Accordion list, collapsed default | CONTEXT.md D-02, D-03 |
| + New Category inline form pattern | CONTEXT.md D-04 |
| Category inline edit on header | CONTEXT.md D-05 |
| + Add expense inline form at bottom | CONTEXT.md D-06 |
| Expense required / optional fields | CONTEXT.md D-07 |
| Expense inline edit on row click | CONTEXT.md D-08 |
| Sage green palette, utility class names | RESEARCH.md + globals.css |
| shadcn preset base-nova, Lucide icons | components.json |
| Font stack (Playfair Display + Inter) | globals.css |
| Currency formatter pattern | dashboard/page.tsx |
| Card, Button, Input, Textarea components | components/ui/ scan |
| Page max-width container | (app)/layout.tsx |
| Error copy pattern (text-destructive) | DecisionQueue.tsx |
| Status badge approach (pill shape) | RESEARCH.md code example + OptionList pattern |
| Paid = sage-100/sage-700 | RESEARCH.md code example |
| Pending = amber-100/amber-700 | RESEARCH.md code example |
| Over-budget = amber-50/amber-200/amber-700 | RESEARCH.md code example |
| Inline confirm delete (no toast library) | RESEARCH.md open question 2 |
| revalidatePath both /budget + /dashboard | CONTEXT.md code_context + RESEARCH.md |
